import styled from "styled-components";

export const StyledChartContainer = styled.div({
    // maxHeight: '80vh',
    // height: `${numDataPoints * 30}px`, // Adjust the height based on the number of data points
    transition: "height 0.3s ease", // Add a smooth transition animation
});